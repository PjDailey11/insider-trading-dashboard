# sec-api.io — corporate Form 4 insider trades
SEC_API_KEY=

# Public.com API secret (settings → API) + brokerage account id for market data
PUBLIC_API_KEY=
PUBLIC_ACCOUNT_ID=

# Optional: force mock/seed data in the browser (default is live)
# NEXT_PUBLIC_DATA_SOURCE=mock
